# How to Use

```bash
python -m veraforge.cli.main compile examples/conversational-agent/conversational-agent.vera --target=fast-agent --output conversational-agent.py
```

or

```bash
python -m veraforge.cli.main compile examples/conversational-agent/conversational-agent.vera --target=ir --output conversational-agent.vf.json
```